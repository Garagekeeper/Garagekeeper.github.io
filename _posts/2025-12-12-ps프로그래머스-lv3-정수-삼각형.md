---
title: "[PS][프로그래머스-lv3] 정수 삼각형"
date: 2025-12-12 22:17:36 +0900
description: ""
categories: [Computer Science, Algorithm] #[upper, lower]
tags: [cpp, ps, algorithm, dp] #must be lower
math: true
mermaid: true
---
[정수 삼각형](https://school.programmers.co.kr/learn/courses/30/lessons/12900)
### 1) 문제
>![](https://velog.velcdn.com/images/garage_keeper/post/9efae0bb-df32-4f70-afec-275a647e0f41/image.png)

<br>


### 2) 문제 분석 및 풀이
#### 1) 설계, 분석
#### 2) 풀이
>```cpp
#include <string>
#include <vector>
>
using namespace std;
>
int solution(vector<vector<int>> triangle) 
{
    int answer = 0;
    >
    vector<vector<int>> dp = triangle;
    >
    for (int i=1; i<triangle.size(); i++)
    {
        for (int j=0; j<triangle[i].size(); j++)
        {
            // 맨 왼쪽, 오른쪽 위에서만 올 수 있음.
            if (j == 0) { dp[i][j] += dp[i-1][0]; }
            // 멘 오른쪽, 왼쪽 위에서만 올 수 있음.
            else if (j == triangle[i].size() - 1) { dp[i][j] += dp[i-1][j-1]; }
            // 중간, 양쪽 중에서 최대값을 선택
            else 
            {
                dp[i][j] += max(dp[i-1][j], dp[i-1][j-1]);
            }
        }
    }
    >
    answer = *max_element(dp.back().begin(), dp.back().end());
    >
    return answer;
}
>```
  

<br>



