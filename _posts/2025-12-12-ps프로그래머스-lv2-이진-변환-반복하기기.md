---
title: "[PS][프로그래머스-lv2] 이진 변환 반복하기기"
date: 2025-12-12 21:52:47 +0900
description: ""
categories: [Computer Science, Algorithm] #[upper, lower]
tags: [cpp, ps, algorithm, implementation] #must be lower
math: true
mermaid: true
---

[이진 변환 반복하기](https://school.programmers.co.kr/learn/courses/30/lessons/70129?language=cpp)
### 1) 문제
>**문제 설명**
> 0과 1로 이루어진 어떤 문자열 x에 대한 이진 변환을 다음과 같이 정의합니다.
>
x의 모든 0을 제거합니다.
x의 길이를 c라고 하면, x를 "c를 2진법으로 표현한 문자열"로 바꿉니다.
예를 들어, x = "0111010"이라면, x에 이진 변환을 가하면 x = "0111010" -> "1111" -> "100" 이 됩니다.
>
0과 1로 이루어진 문자열 s가 매개변수로 주어집니다. s가 "1"이 될 때까지 계속해서 s에 이진 변환을 가했을 때, 이진 변환의 횟수와 변환 과정에서 제거된 모든 0의 개수를 각각 배열에 담아 return 하도록 solution 함수를 완성해주세요.
>
>**제한 사항**
>s의 길이는 1 이상 150,000 이하입니다.
s에는 '1'이 최소 하나 이상 포함되어 있습니다.
>
>**입 출력 예**
s:"110010101001"	result:[3,8]
s:"01110"			result:[3,3]
s:"1111111"			result:[4,1]

<br>


### 2) 문제 분석 및 풀이
#### 1) 설계, 분석
#### 2) 풀이
>```cpp
#include <string>
#include <vector>
>
using namespace std;
int deci2biCnt = 0;
>
// 2진법 변환
string Deci2bi(int n)
{
    string binary ="";
    // 2로 나눈 나머지가 사용됨
    while(n != 0)
    {
        binary += (n % 2) +'0';
        n /= 2;
    }
    deci2biCnt++;
    // 가장 마지막부터 사용하니까 뒤집어서 반환
    return  string(binary.rbegin(), binary.rend());
}
>
vector<int> solution(string s) 
{
    vector<int> answer;
    string currStr(s);
    int cnt = 0;
    // 문제 조건을 만족할 때까지 반복
    while(currStr != "1")
    {
        string temp = "";  
        >
  		// 0을 제거
        for (auto& e : currStr)
        {
            if (e != '0') temp+=e;
            else cnt++;
        }
        >
  		// 0을 제거한 문제의 길이를 이진 변환
        currStr = Deci2bi(temp.size());
    }
  	// 이진 변환 횟수, 0을 제거한 횟수
    return {deci2biCnt, cnt};
}
>```
  

<br>

